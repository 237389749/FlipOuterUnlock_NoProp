name: CI Build

on:
  workflow_dispatch:
  push:
    branches: [ main ]
    tags: [ v* ]
  pull_request:
    types: [ opened, synchronize, reopened ]
    paths:
      - 'app/**'
      - '.github/workflows/**'
      - 'gradle/**'
      - 'gradlew*'
      - 'build.gradle*'
      - 'settings.gradle*'
      - 'gradle.properties'

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - uses: gradle/actions/setup-gradle@v5
        with:
          cache-cleanup: on-success

      - name: Run lint
        continue-on-error: true
        run: ./gradlew :app:lintDebug --continue

  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write          # 创建 Release + 上传 asset 需要(GITHUB_TOKEN)
    steps:
      - uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Check signing key availability
        id: signing
        run: |
          if [ -n "${{ secrets.KEYSTORE }}" ] && [ -n "${{ secrets.KEYSTORE_PASSWORD }}" ] && [ -n "${{ secrets.ALIAS }}" ] && [ -n "${{ secrets.KEY_PASSWORD }}" ]; then
            echo "available=true" >> $GITHUB_OUTPUT
          else
            echo "available=false" >> $GITHUB_OUTPUT
          fi

      - name: Restore signing key
        if: steps.signing.outputs.available == 'true'
        run: |
          echo "${{ secrets.KEYSTORE }}" | base64 --decode > key.jks
          echo "androidStoreFile=key.jks" >> gradle.properties
          echo "androidStorePassword=${{ secrets.KEYSTORE_PASSWORD }}" >> gradle.properties
          echo "androidKeyAlias=${{ secrets.ALIAS }}" >> gradle.properties
          echo "androidKeyPassword=${{ secrets.KEY_PASSWORD }}" >> gradle.properties

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - uses: gradle/actions/setup-gradle@v5
        with:
          cache-cleanup: on-success

      - name: Build (Release)
        if: steps.signing.outputs.available == 'true'
        run: |
          echo 'org.gradle.caching=true' >> ~/.gradle/gradle.properties
          echo 'org.gradle.parallel=true' >> ~/.gradle/gradle.properties
          echo 'org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8' >> ~/.gradle/gradle.properties
          ./gradlew :app:assembleRelease
          echo "APK_GLOB=app/build/outputs/apk/release/*.apk" >> $GITHUB_ENV

      - name: Build (Debug)
        if: steps.signing.outputs.available == 'false'
        run: |
          echo 'org.gradle.caching=true' >> ~/.gradle/gradle.properties
          echo 'org.gradle.parallel=true' >> ~/.gradle/gradle.properties
          echo 'org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8' >> ~/.gradle/gradle.properties
          ./gradlew :app:assembleDebug
          echo "APK_GLOB=app/build/outputs/apk/debug/*.apk" >> $GITHUB_ENV

      - name: Set artifact name
        id: artifact_name
        run: |
          if [ "${{ github.ref_type }}" = "tag" ]; then
            echo "suffix=${{ github.ref_name }}" >> $GITHUB_OUTPUT
          else
            echo "suffix=${{ github.sha }}" >> $GITHUB_OUTPUT
          fi

      - name: Package property-layer module (zip, Magisk/KSU flashable)
        if: github.ref_type == 'tag'
        run: |
          (cd property_layer/flip_prop && zip -r ../../flipunlock_prop.zip .)
          echo "ZIP_GLOB=flipunlock_prop.zip" >> $GITHUB_ENV

      - name: Upload APK (artifact)
        uses: actions/upload-artifact@v5
        with:
          path: ${{ env.APK_GLOB }}
          name: FlipOuterUnlock_${{ steps.artifact_name.outputs.suffix }}

      - name: Create Release
        if: github.ref_type == 'tag'
        uses: softprops/action-gh-release@v2
        with:
          files: |
            ${{ env.APK_GLOB }}
            ${{ env.ZIP_GLOB }}
          name: FlipOuterUnlock ${{ github.ref_name }}
